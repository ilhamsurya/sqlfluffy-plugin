[sqlfluff:rules:PolicyGroup_L001]
forbidden_columns = id, country, sample_data

[sqlfluff:rules:PolicyGroup_L002]
cols_to_check = id, country, sample_data
valid_data_types = bigint, int, tinyint, char, varchar, 

